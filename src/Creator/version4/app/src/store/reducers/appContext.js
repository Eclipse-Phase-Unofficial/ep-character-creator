
const INITIAL_STATE = {
  firstLoad: true,
}

const appContext = (state = {}, action) => {
  switch(action.type){
    // TODO wrote reducer and actions
    default:
      return state
  }
}


export default appContext
